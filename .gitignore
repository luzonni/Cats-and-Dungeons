# Build
build/
out/
bin/
*.class
*.jar
!libs/*.jar
!gradle/wrapper/gradle-wrapper.jar

# Gradle
.gradle/
gradle-app.setting

# Preferências locais do jogador, geradas em tempo de execução
config.json
config.json.tmp
config.json.bak

# As corridas interrompidas, gravadas em tempo de execução. É estado de UMA
# pessoa jogando, e não do projeto: versioná-las faria o jogo de cada um começar
# dentro da partida de outro. O corrida.json solto é da versão de um espaço só,
# migrado para saves/ na primeira execução.
saves/
corrida.json
corrida.json.tmp

# IDEs
.idea/
*.iml
*.ipr
*.iws
.vscode/
.settings/
.classpath
.project

# Sistema operacional
Thumbs.db
Desktop.ini
.DS_Store

# Documentação
documentation/

# Packs de arte de terceiros, baixados na hora de gerar.
# Mesma regra do pacote do Kenney: o que entra no repositório é o PNG derivado,
# não o pacote inteiro. Ver docs/TERCEIROS.md.
tools/assets/fonte/
